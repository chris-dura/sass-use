{
  "name": "sass-use-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run library && npm run legacy && npm run consumer",
    "library": "sass src/library",
    "legacy": "sass src/legacy",
    "consumer": "sass src/consumer",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.23.7",
    "sass-migrator": "^1.0.1"
  }
}
